# SCENIC ED CUDA 12.8 / NVIDIA H20 environment.
#
# Recommended base on the H20 machine:
#   conda create -n scenic-ed python=3.10 -y
#   conda activate scenic-ed
#   python -m pip install -U pip
#   python -m pip uninstall -y torch torchvision torchaudio
#   python -m pip install -r requirements.txt
#
# Important:
#   Driver 570.124.06 is a CUDA 12.8 driver. The PyTorch wheel must also be a
#   CUDA 12.8 wheel. If pip installs a newer CUDA wheel, PyTorch can report
#   "CUDA driver is too old" even though nvidia-smi shows CUDA 12.8.

--extra-index-url https://download.pytorch.org/whl/cu128

certifi==2026.5.20
accelerate>=1.1.0
huggingface_hub==1.17.0
numpy>=1.26
onnx>=1.16
onnxruntime-gpu>=1.18; platform_system == "Linux"
onnxruntime>=1.18; platform_system != "Linux"
onnxscript>=0.3
openai==2.38.0
pytest==9.0.3
requests==2.34.2
safetensors>=0.4.5
sentencepiece==0.2.1
torch==2.8.0+cu128; platform_system == "Linux"
torch==2.8.0; platform_system != "Linux"
tqdm==4.67.3
transformers==5.9.0
urllib3==2.7.0
